# Requirements

## Dependencies
- **FastAPI**: Web framework to build the API endpoints
- **Uvicorn**: Web server that runs the FastAPI app

## Setup
1. Create virtual environment and activate it
2. Install last version of the dependencies
3. Create a requirements.txt in the root of the project with the installed dependencies